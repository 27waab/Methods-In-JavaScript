# 📦 JavaScript Arrays – Complete Cheat Sheet

## =========================================
## 📌 دوال وميثودات المصفوفات (Array Prototype)
## =========================================

### 🟦 1) التعديل على المصفوفة (Mutating)

| الميثود | وش يسوي؟ | مثال |
|--------|-----------|-------|
| **push()** | يضيف عنصر بنهاية المصفوفة. | `arr.push(4)` |
| **pop()** | يحذف آخر عنصر ويرجّعه. | `arr.pop()` |
| **shift()** | يحذف أول عنصر ويرجّعه. | `arr.shift()` |
| **unshift()** | يضيف عنصر بالبداية. | `arr.unshift(0)` |
| **splice()** | حذف/إضافة بموضع معيّن. | `arr.splice(1,2)` |
| **sort()** | ترتيب المصفوفة. | `arr.sort()` |
| **reverse()** | يعكس الترتيب. | `arr.reverse()` |
| **fill()** | يملأ المصفوفة بقيمة معينة. | `arr.fill(0)` |
| **copyWithin()** | نسخ جزء لمكان آخر داخل نفس المصفوفة. | `arr.copyWithin(1,3)` |

---

### 🟩 2) لا تعدل المصفوفة الأصلية (Non-Mutating)

| الميثود | وش يسوي؟ | مثال |
|--------|-----------|-------|
| **slice()** | يرجّع جزء بدون تغيير الأصل. | `arr.slice(1,3)` |
| **concat()** | دمج مصفوفتين. | `arr.concat(b)` |
| **join()** | دمج العناصر داخل نص. | `arr.join("-")` |
| **includes()** | يتحقق من وجود قيمة. | `arr.includes(3)` |
| **indexOf()** | أول ظهور للقيمة. | `arr.indexOf(2)` |
| **lastIndexOf()** | آخر ظهور للقيمة. | `arr.lastIndexOf(2)` |
| **toString()** | يحولها لنص. | `arr.toString()` |
| **flat()** | يفرد المصفوفات المتداخلة. | `[1,[2,3]].flat()` |
| **flatMap()** | map + flat بوقت واحد. | `arr.flatMap(x => [x, x*2])` |

---

### 🟨 3) ميثودات التكرار (Iteration Methods)

| الميثود | ماذا يفعل؟ | مثال |
|--------|-------------|-------------|
| **forEach()** | يكرر على كل عنصر بدون return. | `arr.forEach(x => console.log(x))` |
| **map()** | يرجع مصفوفة جديدة بالقيم المعدلة. | `arr.map(x => x*2)` |
| **filter()** | يرجع العناصر اللي تحقق الشرط. | `arr.filter(x => x>3)` |
| **find()** | يرجع أول عنصر يحقق الشرط. | `arr.find(x => x>3)` |
| **findIndex()** | يرجع فهرس أول عنصر يحقق الشرط. | `arr.findIndex(x=>x>3)` |
| **some()** | هل فيه عنصر يحقق الشرط؟ | `arr.some(x=>x>3)` |
| **every()** | هل كل العناصر تحقق الشرط؟ | `arr.every(x=>x>3)` |
| **reduce()** | يدمج/يجمع كل القيم في قيمة واحدة. | `arr.reduce((a,b)=>a+b)` |
| **reduceRight()** | نفس reduce لكن من اليمين. | `arr.reduceRight((a,b)=>a+b)` |

---

### 🟫 4) تحويل المصفوفات (Transform)

| الميثود | ماذا يفعل؟ | مثال |
|--------|-------------|-------------|
| **toLocaleString()** | نص حسب لغة النظام. | `arr.toLocaleString()` |
| **entries()** | مولّد يرجع [index, value]. | `for (let e of arr.entries())` |
| **keys()** | مولّد للفهارس. | `arr.keys()` |
| **values()** | مولّد للقيم. | `arr.values()` |

---

<br>

## =========================================
## 🧩 دوال Array.* (Static Methods)
## =========================================

### 🔷 دوال على مستوى Array نفسها (مو على نسخة)

| الدالة Array.* | ماذا تفعل؟ | مثال |
|----------------|------------|---------|
| **Array.isArray()** | يتحقق إذا القيمة مصفوفة. | `Array.isArray(arr)` |
| **Array.from()** | يحول كائن يشبه المصفوفة إلى مصفوفة. | `Array.from("abc") // ['a','b','c']` |
| **Array.of()** | يصنع مصفوفة من القيم. | `Array.of(1,2,3)` |
