# 🔢 Number & 🧮 Math – JavaScript Cheat Sheet

## =========================================
## 🔢 Number Methods & Properties
## =========================================

### 📌 الدوال (Methods)

| الاسم | وش يسوي؟ | مثال مختصر |
|-------|----------|------------|
| **Number()** | يحوّل أي قيمة إلى رقم. | `Number("10") // 10` |
| **Number.isNaN()** | يتحقق إذا القيمة NaN بدون تحويل. | `Number.isNaN(NaN) // true` |
| **Number.isFinite()** | يتحقق إذا القيمة رقم محدود. | `Number.isFinite(10) // true` |
| **Number.isInteger()** | يتحقق إذا القيمة عدد صحيح. | `Number.isInteger(3) // true` |
| **Number.parseInt()** | يحوّل نص لرقم صحيح. | `Number.parseInt("12px") // 12` |
| **Number.parseFloat()** | يحوّل نص لرقم عشري. | `Number.parseFloat("3.14m") // 3.14` |
| **toString()** | يحوّل الرقم لنص. | `(15).toString() // "15"` |
| **toFixed()** | يحدد الخانات بعد الفاصلة. | `(3.14159).toFixed(2) // "3.14"` |
| **toPrecision()** | يحدد عدد الخانات ككل. | `(3.14159).toPrecision(3) // "3.14"` |
| **toExponential()** | صيغة علمية. | `(1500).toExponential() // "1.5e+3"` |
| **valueOf()** | يرجّع القيمة الرقمية الفعلية. | `(10).valueOf() // 10` |

---

### 📌 الخصائص (Properties)

| الاسم | وش يسوي؟ | مثال مختصر |
|-------|----------|------------|
| **Number.MAX_VALUE** | أكبر رقم ممكن. | `Number.MAX_VALUE` |
| **Number.MIN_VALUE** | أصغر رقم موجب. | `Number.MIN_VALUE` |
| **Number.MAX_SAFE_INTEGER** | أكبر رقم صحيح آمن. | `Number.MAX_SAFE_INTEGER` |
| **Number.MIN_SAFE_INTEGER** | أصغر رقم صحيح آمن. | `Number.MIN_SAFE_INTEGER` |
| **Number.NaN** | قيمة Not-a-Number. | `Number.NaN` |
| **Number.POSITIVE_INFINITY** | ∞ | `Number.POSITIVE_INFINITY` |
| **Number.NEGATIVE_INFINITY** | -∞ | `Number.NEGATIVE_INFINITY` |

<br>

---

## =========================================
## 🧮 Math Methods & Constants
## =========================================

### 🔢 الدوال (Methods)

| الاسم | وش يسوي؟ | مثال مختصر |
|-------|----------|------------|
| **Math.abs()** | القيمة المطلقة. | `Math.abs(-5) // 5` |
| **Math.round()** | تقريب عادي. | `Math.round(4.6) // 5` |
| **Math.floor()** | لتحت دائمًا. | `Math.floor(4.9) // 4` |
| **Math.ceil()** | لفوق دائمًا. | `Math.ceil(4.1) // 5` |
| **Math.trunc()** | يشيل الجزء العشري. | `Math.trunc(4.9) // 4` |
| **Math.pow()** | قوة (أس). | `Math.pow(2,3) // 8` |
| **Math.sqrt()** | الجذر التربيعي. | `Math.sqrt(16) // 4` |
| **Math.cbrt()** | الجذر التكعيبي. | `Math.cbrt(27) // 3` |
| **Math.max()** | أكبر قيمة. | `Math.max(1,5,3) // 5` |
| **Math.min()** | أصغر قيمة. | `Math.min(1,5,3) // 1` |
| **Math.random()** | رقم عشوائي بين 0 و1. | `Math.random()` |
| **Math.sign()** | إشارة الرقم. | `Math.sign(-10) // -1` |
| **Math.hypot()** | √(a² + b² + …). | `Math.hypot(3,4) // 5` |
| **Math.clz32()** | عدد الأصفار اليسار في 32-bit. | `Math.clz32(1) // 31` |

---

### 🧩 اللوغاريتمات

| الاسم | وش يسوي؟ | مثال مختصر |
|-------|----------|------------|
| **Math.log()** | ln(x). | `Math.log(1) // 0` |
| **Math.log10()** | log₁₀(x). | `Math.log10(100) // 2` |
| **Math.log2()** | log₂(x). | `Math.log2(8) // 3` |
| **Math.expm1()** | ‎eˣ − 1‎ | `Math.expm1(1)` |
| **Math.log1p()** | ln(1 + x). | `Math.log1p(0)` |

---

### 📐 دوال المثلثات

| الاسم | وش يسوي؟ | مثال مختصر |
|-------|----------|------------|
| **Math.sin()** | الجيب. | `Math.sin(Math.PI/2) // 1` |
| **Math.cos()** | جيب التمام. | `Math.cos(0) // 1` |
| **Math.tan()** | الظل. | `Math.tan(Math.PI/4) // 1` |
| **Math.asin()** | عكس الجيب. | `Math.asin(1)` |
| **Math.acos()** | عكس جيب التمام. | `Math.acos(1)` |
| **Math.atan()** | عكس الظل. | `Math.atan(1)` |
| **Math.atan2()** | زاوية (y,x). | `Math.atan2(1,1)` |

---

### 📌 الثوابت (Constants)

| الاسم | وش يسوي؟ | مثال مختصر |
|-------|----------|------------|
| **Math.PI** | π | `Math.PI` |
| **Math.E** | ثابت e | `Math.E` |
| **Math.LN2** | ln(2) | `Math.LN2` |
| **Math.LN10** | ln(10) | `Math.LN10` |
| **Math.LOG2E** | log₂(e) | `Math.LOG2E` |
| **Math.LOG10E** | log₁₀(e) | `Math.LOG10E` |
| **Math.SQRT2** | √2 | `Math.SQRT2` |
| **Math.SQRT1_2** | 1/√2 | `Math.SQRT1_2` |
